import styled from 'styled-components';
import { StyledFlexWrapper } from '../../styled/flex';

export const StyledPieWrapper = styled(StyledFlexWrapper)`
  background-color: rgb(205, 197, 211);
  border-radius: 10px;
  width: 100%;
`;
