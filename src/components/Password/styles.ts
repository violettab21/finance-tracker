import { styled } from 'styled-components';

export const StyledPassword = styled.div`
  position: relative;
  width: 100%;

  button {
    position: absolute;
    right: 5px;
    border: none;
    background: none;
    top: 35%;
  }
`;
